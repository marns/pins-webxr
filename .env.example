# Signaling endpoint (full URL to POST /offer)
# VITE_OFFER_URL=http://localhost:8080/offer

# ICE servers (optional). JSON array of RTCIceServer objects only.
# VITE_ICE_SERVERS=[{"urls":"stun:stun.l.google.com:19302"}]
